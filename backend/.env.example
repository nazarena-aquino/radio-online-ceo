# Servidor
PORT=3001
NODE_ENV=development

# Base de datos (Neon PostgreSQL)
DATABASE_URL=postgresql://username:password@ep-xxx.us-east-1.aws.neon.tech/radio_db?sslmode=require

# CORS - URL del frontend
FRONTEND_URL=http://localhost:5173

# Autenticación — cambiá estos valores por los tuyos
JWT_SECRET=cambia_esto_por_una_clave_secreta_larga_y_segura
ADMIN_USER=admin
ADMIN_PASSWORD=admin123
